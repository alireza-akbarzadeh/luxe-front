/**
 * Canonical storefront content width — matches navbar, footer, and page bodies.
 * Defined in `globals.css` as the `app-container` utility.
 */
export const siteContainerClass = 'app-container';
