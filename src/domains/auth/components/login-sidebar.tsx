'use client';

import { AuthBrandPanel } from './auth-brand-panel';

/** @deprecated Prefer `AuthBrandPanel` — kept for existing imports. */
export function LoginSidebar() {
  return <AuthBrandPanel />;
}
