'use client';

import { PersonalizationFeaturesSection } from '@/domains/apps/components/personalization-features-section';

/** Homepage band — introduces mood, goal, and memory shopping before shoppers reach /apps. */
export function PersonalizationDiscoverySection() {
  return <PersonalizationFeaturesSection />;
}
