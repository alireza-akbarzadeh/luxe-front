/**
 * Static storefront links merged into the main nav when they are not yet
 * provided by the nav menus API (Collections, Gift Cards, etc.).
 */
export const navSupplementLinks = [
  { label: 'Collections', href: '/collections' },
  { label: 'Gift Cards', href: '/gift-cards' }
] as const;

export const navDiscoverLinks = [
  { label: 'Collections', href: '/collections' },
  { label: 'Gift Cards', href: '/gift-cards' }
] as const;
